import { EditorSelectorDirective } from './editor-selector.directive';

/*describe('EditorSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new EditorSelectorDirective();
    expect(directive).toBeTruthy();
  });
});*/
